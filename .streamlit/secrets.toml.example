# Streamlit Cloud secrets — copy this to your app's Secrets settings panel
# on share.streamlit.io (do NOT commit the real .streamlit/secrets.toml with credentials)

MONGO_URI = "mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority"
MONGO_DB_NAME = "mewaka_program_metrics"
